import { createTheme } from '@mui/material'

export const colors = {
  'accent': '#114954',
  'default': '#394446'
}

export const theme = createTheme({
  palette: {
    primary: {
      main: '#114954'
    }
  }
})
